using System;

namespace CXUtils.Domain
{
    /// <summary>
    ///     Implements a simple wrapper around a object that is pooled
    /// </summary>
    public interface IPoolItem : IDisposable
    {
    }
}
